services:
  hubp:
    image: minyoad/hubp:latest
    # image：ghcr.io/minyoad/hubp:latest
    container_name: hubp
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
    ports:
      - "8080:45000"
